"""The AI layer's first occupant: a test box (INSPECT-0319, the #28 first cut).

Nothing in this package reaches a data path, a wire event, or a number. It exists so
the first real feature arrives into a client, a gate call, and a disclosure row that
already work — see ``app.core.ai`` for the doctrine every module here is bound by.
"""
